const config = {

    'locations': {
        'settings': __dirname + '/../data/settings.json',
    },

    'exchanges': ['bitmex', 'binance', 'bybit', 'verbit'],

    'credentials': {
        'username': 'secret',
        'password': 'password',
    },

}

module.exports = config;