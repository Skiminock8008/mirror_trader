const config = {

    'locations': {
        'settings': __dirname + '/../data/settings.json',
    },

    'credentials': {
        'username': 'secret',
        'password': 'password',
    },

}

module.exports = config;